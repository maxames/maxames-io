{
  "name": "maxames-io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter main dev",
    "dev:notes": "pnpm --filter notes dev",
    "build": "pnpm --filter main build",
    "build:notes": "pnpm --filter notes build",
    "preview": "pnpm --filter main preview",
    "preview:notes": "pnpm --filter notes preview"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@biomejs/biome",
      "@tailwindcss/oxide",
      "esbuild",
      "sharp"
    ]
  }
}
